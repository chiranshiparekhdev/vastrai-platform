import Link from "next/link"
import { ArrowRight, CheckCircle2, Clock3, Plus, Sparkles } from "lucide-react"

export function PageHeading({ eyebrow, title, description, action }: { eyebrow?: string; title: string; description?: string; action?: React.ReactNode }) { return <div className="mb-8 flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div>{eyebrow && <p className="mb-2 text-xs font-bold uppercase tracking-[0.2em] text-primary">{eyebrow}</p>}<h1 className="font-display text-3xl font-bold tracking-tight md:text-4xl">{title}</h1>{description && <p className="mt-2 max-w-xl text-sm leading-6 text-muted-foreground">{description}</p>}</div>{action}</div> }
export function EmptyState({ title, description, href, actionLabel = "Get started" }: { title: string; description: string; href?: string; actionLabel?: string }) { return <div className="flex min-h-[260px] flex-col items-center justify-center rounded-2xl border border-dashed border-border bg-white/60 px-6 text-center"><div className="mb-4 grid size-12 place-items-center rounded-2xl bg-[#f5e9dc] text-primary"><Sparkles /></div><h2 className="font-display text-xl font-bold">{title}</h2><p className="mt-2 max-w-sm text-sm leading-6 text-muted-foreground">{description}</p>{href && <Link href={href} className="mt-5 inline-flex items-center gap-2 rounded-lg bg-primary px-4 py-2.5 text-sm font-bold text-primary-foreground transition-opacity hover:opacity-90">{actionLabel}<ArrowRight /></Link>}</div> }
export function StatusPill({ status }: { status: "ready" | "processing" | "draft" }) { const config = { ready: ["Ready", CheckCircle2], processing: ["Processing", Clock3], draft: ["Draft", Clock3] } as const; const [label, Icon] = config[status]; return <span className="inline-flex items-center gap-1.5 rounded-full bg-muted px-2.5 py-1 text-xs font-semibold text-muted-foreground"><Icon data-icon="inline-start" />{label}</span> }
export function PrimaryLink({ href, children }: { href: string; children: React.ReactNode }) { return <Link href={href} className="inline-flex items-center gap-2 rounded-lg bg-primary px-4 py-2.5 text-sm font-bold text-primary-foreground shadow-sm transition-all hover:-translate-y-0.5 hover:shadow-md"><Plus data-icon="inline-start" />{children}</Link> }
